from typing import Any


config: dict[str, Any] = {
    'epochs': 10,
    'learning_rate': 0.001,
    'batch_size': 32,
    'optimizer': 'adam',
    'seed': 42,
    'activation': 'sigmoid'
    # Other configs params
}
